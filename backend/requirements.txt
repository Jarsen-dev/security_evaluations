# --- Framework web ---------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Base de datos ---------------------------------------------------------
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# --- Configuración y validación --------------------------------------------
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0

# --- Seguridad -------------------------------------------------------------
passlib[bcrypt]==1.7.4
# passlib 1.7.4 lee `bcrypt.__about__`, atributo eliminado en bcrypt 4.1:
# fijar <4.1 evita el warning ruidoso en cada hash.
bcrypt==4.0.1
PyJWT==2.10.1

# --- Exportaciones ---------------------------------------------------------
openpyxl==3.1.5
python-pptx==1.0.2
# PDF imprimible del cuestionario. reportlab es Python puro: no arrastra
# dependencias del sistema al contenedor, a diferencia de WeasyPrint.
reportlab==4.2.5
# Etiquetas QR de los extintores. Python puro y sin dependencias del sistema,
# igual que reportlab; genera la matriz y Pillow —que ya está abajo— la pinta.
# Se genera en el backend y no en el navegador porque la etiqueta mide 3x3 cm
# EXACTOS y el tamaño físico de un `window.print()` depende de la impresora.
qrcode==8.0
# openpyxl solo puede insertar imágenes (las evidencias del control de Rayser)
# si Pillow está presente. Llegaba de rebote como dependencia de python-pptx;
# se fija explícito porque ahora hay código que depende de él.
Pillow==11.0.0

# --- Rondines de seguridad -------------------------------------------------
# Códigos QR de los puntos de control, para la hoja imprimible. segno es
# Python puro: no arrastra dependencias del sistema al contenedor.
segno==1.6.1

# --- Correo ----------------------------------------------------------------
# El smtplib de la biblioteca estándar es bloqueante: mientras habla con el
# servidor de correo detendría el bucle de eventos y con él toda la API.
aiosmtplib==3.0.2

# --- Recepciones por foto (OCR + clasificación) ----------------------------
# Envoltorio del binario `tesseract` que instala el Dockerfile. Pillow ya
# está arriba y es lo que abre y normaliza la imagen antes de leerla.
pytesseract==0.3.13

# scikit-learn rompe la costumbre del proyecto de "solo Python puro": arrastra
# numpy y scipy y engorda la imagen. Se acepta a propósito porque el umbral
# del clasificador (0.20) y la configuración del vectorizador (`char_wb`,
# n-gramas 3-5) están calibrados contra ESTA implementación de TF-IDF con
# documentos reales; rehacerla a mano invalidaría esa calibración y volvería
# a dejar el umbral en adivinanza.
scikit-learn==1.6.0
numpy==2.2.0

# Cliente HTTP asíncrono para hablar con Ollama. `requests` es bloqueante y
# detendría el bucle de eventos durante los hasta 90 s que puede tardar la
# estructuración (mismo motivo que aiosmtplib).
httpx==0.28.1

# --- Pruebas ---------------------------------------------------------------
# Solo cubren la lógica pura del pipeline de recepciones (clasificación,
# parseo, rutas de campos nulos, degradación): no necesitan Tesseract, ni el
# LLM, ni base de datos, y por eso corren en cualquier lado.
pytest==8.3.4
pytest-asyncio==0.25.0
