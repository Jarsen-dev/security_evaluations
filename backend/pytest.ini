[pytest]
testpaths = tests
asyncio_mode = auto
# El contenedor de producción corre como un usuario sin permiso de escritura
# sobre /app, y el caché de pytest no aporta nada aquí.
addopts = -p no:cacheprovider
asyncio_default_fixture_loop_scope = function
filterwarnings =
    error::DeprecationWarning
